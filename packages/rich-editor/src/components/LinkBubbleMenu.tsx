/**
 * Link bubble-menu placeholder. Tiptap v3 moved BubbleMenu out of `@tiptap/react`,
 * so this iteration ships without it — the toolbar Link button still works via
 * URL prompt. A proper bubble-menu (edit / remove buttons that appear next to
 * a link when the cursor is inside it) lands in a later pass.
 */
export function LinkBubbleMenu() {
  return null;
}
